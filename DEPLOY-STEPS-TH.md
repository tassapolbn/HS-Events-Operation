# วิธีติดตั้งรอบนี้ แบบละเอียด

เอกสารนี้ทำเฉพาะสองเรื่องที่เหลือ คือ deploy Edge Function 3 ตัว และเพิ่ม Secret 2 ตัว
ทำผ่านหน้าเว็บ Supabase ทั้งหมด ไม่ต้องลงโปรแกรมอะไรเพิ่ม

ใช้เวลาประมาณ 15 นาที

---

## เตรียมตัวก่อนเริ่ม

เปิด 2 หน้าต่างไว้คู่กัน จะสลับไปมาง่ายกว่า

**หน้าต่างที่ 1 คือเบราว์เซอร์**

1. เข้า https://supabase.com/dashboard
2. ล็อกอิน
3. คลิกเลือกโปรเจกต์ของโรงเรียน
4. ดูเมนูด้านซ้าย คลิก **Edge Functions** (ไอคอนรูปสายฟ้า)

**หน้าต่างที่ 2 คือ File Explorer**

เปิดโฟลเดอร์นี้

```
C:\Users\ASUS\OneDrive\เดสก์ท็อป\hs-opt\HS-Events-Operation\supabase\functions
```

จะเห็น 3 โฟลเดอร์ย่อย แต่ละโฟลเดอร์มีไฟล์ชื่อ `index.ts` อยู่ข้างใน

---

## ส่วนที่ 1 อัปเดต send-notification

ตัวนี้มีอยู่ในระบบแล้ว แค่เอาโค้ดใหม่ไปทับ

### 1.1 คัดลอกโค้ดจากเครื่อง

1. เปิดไฟล์ `supabase\functions\send-notification\index.ts`
   คลิกขวาที่ไฟล์ เลือก **Open with** แล้วเลือก **Notepad** (หรือ VS Code ถ้ามี)
2. กด **Ctrl+A** เพื่อเลือกทั้งหมด
3. กด **Ctrl+C** เพื่อคัดลอก

### 1.2 วางลงในเว็บ

1. ในหน้า Edge Functions คลิกที่ชื่อ **send-notification** ในรายการ
2. มองหาปุ่มแก้โค้ด ชื่อจะเป็น **Code** หรือ **Edit function** อยู่แถวบน
3. คลิกในช่องโค้ดสีเข้ม แล้วกด **Ctrl+A** เพื่อเลือกโค้ดเดิมทั้งหมด
4. กด **Ctrl+V** เพื่อวางทับ
5. คลิกปุ่ม **Deploy** (หรือ **Deploy function**) ด้านล่างหรือมุมขวาบน
6. รอประมาณ 10 ถึง 30 วินาที จะขึ้นข้อความว่าสำเร็จ

> ถ้าหาปุ่มแก้โค้ดไม่เจอ ให้ลบฟังก์ชันเดิมทิ้งแล้วสร้างใหม่ตามวิธีในส่วนที่ 2
> โดยใช้ชื่อเดิมคือ `send-notification` ผลลัพธ์เหมือนกัน

---

## ส่วนที่ 2 สร้าง request-password-reset

ตัวนี้เป็นของใหม่ ยังไม่เคยมีในระบบ

### 2.1 สร้างฟังก์ชันเปล่า

1. กลับไปหน้า **Edge Functions**
2. คลิกปุ่ม **Deploy a new function**
3. เลือก **Via Editor**
4. ช่องชื่อฟังก์ชัน พิมพ์ให้ตรงเป๊ะทุกตัวอักษร

```
request-password-reset
```

ชื่อต้องตรงเป๊ะ ถ้าพิมพ์ผิดแม้แต่ตัวเดียว เว็บจะเรียกฟังก์ชันไม่เจอ

### 2.2 วางโค้ด

1. เปิดไฟล์ `supabase\functions\request-password-reset\index.ts` ด้วย Notepad
2. **Ctrl+A** แล้ว **Ctrl+C**
3. กลับไปที่เว็บ คลิกในช่องโค้ด กด **Ctrl+A** เพื่อลบโค้ดตัวอย่างที่ระบบใส่มาให้
4. กด **Ctrl+V**
5. คลิก **Deploy function**

### 2.3 ปิดการตรวจ JWT ของตัวนี้

ขั้นนี้สำคัญ ถ้าไม่ทำ ปุ่มลืมรหัสผ่านจะกดแล้วไม่มีอะไรเกิดขึ้น
เพราะระบบจะไม่ยอมให้คนที่ยังไม่ได้ล็อกอินเรียกฟังก์ชันนี้
ซึ่งขัดกับความจริงที่ว่าคนลืมรหัสผ่านย่อมล็อกอินไม่ได้

1. เปิดหน้าของฟังก์ชัน **request-password-reset**
2. มองหาแท็บหรือหัวข้อ **Settings** หรือ **Details**
3. หาตัวเลือกชื่อ **Enforce JWT Verification** หรือ **Verify JWT**
4. ปิดสวิตช์นั้น แล้วบันทึก

**ถ้าหาไม่เจอ** ไม่ต้องกังวล ข้ามไปทำส่วนอื่นให้ครบก่อน
แล้วบอกผม ผมจะให้วิธีสำรองซึ่งใช้โปรแกรมเสริมอีกตัวหนึ่ง
รายละเอียดอยู่ท้ายเอกสารนี้

ปิดเฉพาะตัวนี้ตัวเดียวเท่านั้น อีกสองตัวห้ามปิด
ฟังก์ชันนี้ไม่คืนข้อมูลอะไรให้คนเรียกเลย ตอบเหมือนกันทุกครั้งไม่ว่าจะมีบัญชีนั้นจริงหรือไม่
จึงเปิดให้เรียกได้โดยไม่ต้องล็อกอินได้อย่างปลอดภัย

---

## ส่วนที่ 3 สร้าง manage-users

ทำเหมือนส่วนที่ 2 ทุกอย่าง ยกเว้นสองจุด

- ชื่อฟังก์ชันคือ

```
manage-users
```

- ใช้โค้ดจากไฟล์ `supabase\functions\manage-users\index.ts`
- **ห้ามปิด** การตรวจ JWT ของตัวนี้ เพราะเป็นฟังก์ชันจัดการบัญชีผู้ใช้
  ต้องให้เฉพาะแอดมินที่ล็อกอินแล้วเท่านั้นที่เรียกได้

---

## ส่วนที่ 4 เพิ่ม Secret 2 ตัว

Secret คือค่าลับที่ฟังก์ชันเรียกใช้ได้แต่คนนอกมองไม่เห็น

1. ในหน้า **Edge Functions** มองหาแท็บ **Secrets**
   ถ้าไม่เจอ ลองที่ **Project Settings** แล้วหาหัวข้อ **Edge Functions** แล้วดู **Secrets**
2. คลิก **Add new secret**
3. ใส่ตัวแรก

| ช่อง | ค่าที่ใส่ |
|---|---|
| Name | `APP_URL` |
| Value | `https://hs-opt.netlify.app` |

4. คลิกบันทึก แล้วคลิก **Add new secret** อีกครั้ง ใส่ตัวที่สอง

| ช่อง | ค่าที่ใส่ |
|---|---|
| Name | `DEFAULT_RECOVERY_EMAIL` |
| Value | `communications.city@headstartphuket.com` |

ชื่อ Secret ต้องพิมพ์ใหญ่ทั้งหมดและมีขีดล่างตรงตามนี้

### ตรวจว่า Secret เดิมยังอยู่ครบ

ในหน้าเดียวกันควรเห็นสองตัวนี้อยู่แล้วจากที่เคยตั้งไว้

- `APPS_SCRIPT_URL`
- `APPS_SCRIPT_SECRET`

ถ้าหายไปตัวใดตัวหนึ่ง อีเมลแจ้งเตือนจะไม่ถูกส่ง บอกผมได้เลย

---

## ส่วนที่ 5 ทดสอบว่าใช้ได้จริง

ทำตามลำดับนี้ ถ้าติดตรงไหนให้หยุดแล้วบอกผม

**ทดสอบที่ 1 ระบบเดิมยังทำงานอยู่**

เปิดเว็บ https://hs-opt.netlify.app ล็อกอินตามปกติ
เข้าอีเวนท์ใดก็ได้ แล้วลองส่งอีเมลแจ้งเตือนหนึ่งครั้ง
ถ้าอีเมลมาถึง แปลว่า `send-notification` ตัวใหม่ทำงานถูกต้อง

**ทดสอบที่ 2 หน้าจัดการบัญชี**

ล็อกอินด้วยบัญชีแอดมิน มองเมนูด้านซ้ายจะมีหัวข้อ **Accounts** เพิ่มขึ้นมา
เปิดเข้าไปแล้วควรเห็นรายชื่อคนในระบบ
ถ้าเห็นรายชื่อ แปลว่า `manage-users` ทำงานถูกต้อง

**ทดสอบที่ 3 ลืมรหัสผ่าน**

ออกจากระบบ แล้วที่หน้าล็อกอินคลิก **ลืมรหัสผ่าน**
ใส่ username หรืออีเมลของตัวเอง แล้วกดส่ง
เปิดกล่องจดหมาย communications.city@headstartphuket.com ดูว่ามีอีเมลเข้ามาไหม

ถ้าไม่มีอีเมลเข้ามา แสดงว่าขั้น 2.3 ยังไม่เรียบร้อย บอกผมได้เลย

---

## วิธีสำรอง ใช้เมื่อหาสวิตช์ JWT ในเว็บไม่เจอ

วิธีนี้ต้องลงโปรแกรม Supabase CLI ในเครื่อง ทำครั้งเดียวจบ

เปิด PowerShell แล้วพิมพ์ทีละบรรทัด

```
npm install -g supabase
supabase login
```

คำสั่งที่สองจะเปิดเบราว์เซอร์ให้อนุญาต กด Authorize

จากนั้นเข้าโฟลเดอร์โปรเจกต์

```
cd "C:\Users\ASUS\OneDrive\เดสก์ท็อป\hs-opt\HS-Events-Operation"
supabase init
```

ถ้าถามว่าจะสร้างไฟล์ทับไหม ตอบ yes ได้

เปิดไฟล์ `supabase\config.toml` ที่เพิ่งถูกสร้าง เลื่อนลงไปล่างสุด แล้วเพิ่มสามบรรทัดนี้

```
[functions.request-password-reset]
verify_jwt = false
```

บันทึกไฟล์ แล้วกลับมาที่ PowerShell

```
supabase link --project-ref รหัสโปรเจกต์ของโรงเรียน
supabase functions deploy request-password-reset
```

รหัสโปรเจกต์หาได้จากแถบที่อยู่ของเบราว์เซอร์ตอนเปิด Supabase Dashboard
จะอยู่ในรูป `https://supabase.com/dashboard/project/รหัสตรงนี้`

---

## สรุปสิ่งที่ต้องทำ

- [ ] อัปเดต `send-notification` ด้วยโค้ดใหม่
- [ ] สร้าง `request-password-reset` และปิดการตรวจ JWT ของตัวนี้
- [ ] สร้าง `manage-users` และไม่ต้องปิดอะไร
- [ ] เพิ่ม Secret `APP_URL`
- [ ] เพิ่ม Secret `DEFAULT_RECOVERY_EMAIL`
- [ ] ทดสอบครบ 3 ข้อ
